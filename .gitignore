# OAuth tokens (contain secrets, must not be committed)
content/.oauth-tokens.json

# Dependencies
node_modules
/.pnp
.pnp.js

# Build
.next/
out/
build
dist

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env (API keys, secrets)
.env
.env*.local

# Claude / AI assistant config (local, not for GitHub)
.claude/
CLAUDE.md

# Google OAuth / service account credential files — never commit
client_secret*.json
*service-account*.json
*credentials*.json

# Adapter config with secrets — lives in ~/.research-repo/ (outside repo), gitignored as safety net
.research-repo/

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# IDE
.idea
.vscode
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Video files - NEVER commit raw or generated videos (hosted on OneDrive/SharePoint/Google Drive)
raw_videos/
*.mov
*.mkv
*.mp4
*.webm
*.avi
*.wmv
/public/videos/*
!/public/videos/.gitkeep
!/public/videos/README.md
/public/reports-portable/
/public/reports-pptx/

# Agent CLI settings (local preference, not for version control)
agent-settings.json
