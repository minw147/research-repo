Let me explain what has changed from my initial discussion with you. Full process is: load full recordings from zoom (can be manually download or auto download with skills), the /research-analysis skill will ask the user for codebook (there will be a global codebook in the tool but people can customize it to their own company product), research plan, any other background knowledge, once all files are ready, it'll write a first draft of the Markdown file with findings that include overall insights, try to answer the research questions in the research plan, and a summary/list of ALL of the clips from the markdown findings. It'll also update the project.json file to note meta data about this project, name, short form name, date, researcher, product, persona, # of sessions etc.

Then the researcher can open the local tool (I called it Report Builder but can rename it). On the UI, it shows the transcript and video on the left(video top, transcript bottom, shows timestamp for each row of transcript, by clicking on the transcript the video play), shows the formatted md file (findings) on the right. And the clips which are formatted as (main transcript of the clip, timestamp, session#, tags) quote cards with all these information. By clicking on the timestamp on the quote card, the video plays. User can toggle between formatted and raw md on the right. Use the raw md to change the findings md file, change text, add a new clip, remove a clip, change duration, start timestamp of the quotes etc. The process of creating a clip is also in the UI, user drag to select/highlight text in the transcript, it'll show a "+ clip" modal/button next to the text, by clicking on it it'll turn the selected the text in to a quote card (by showing it differently on the transcript) and then the user can drag it to the right (recommend raw markdown), user will see the text and duration there, and if switch back to formatted the quote card renders. Plus all of the existing quote cards will show on the left. In short, the left transcript view is generated based on the findigns.md or the tags.md (which I'll describe later), so even if you delete a clip on the left, it can still show on the markdown file. And if the user clicks on refresh on the markdown file that clip will show up again on the left transcript. I think this is okay since it's really just the md feels that we are working on the transcript is just a tool to create the clips.

And here is where the magic comes in, since Cursor or Claude or any AI agent you use can modify the markdown file you're seeing, you can ask cursor/Claude to rewrite it/ modify it/append it based on your prompt. Like there's an extra theme you want it to look into, it can use that theme/code to review the transcripts and add all of the quotes related to that them to the findings.md. And once that's done, you hit refresh on the UI and the markdown will update with these changes. In short, you have an AI assistant (Claude code/Cursor) without having to pay extra for AI services. Which is great for people using it within the company, or people who don't' want to have to pay token by API.

One the researcher finished the findigns.md with the help of AI, they click on "Next" to generate the report, it'll generate a mdx file with all of the clips rendered as actual clips that you can click and play. At this point we're still not clipping the clips yet, we're still doing the play from timestamp and stop at the end of that clip based on the duration info which makes it easy for researcher to go back to findings.md to edit and reaerate the mdx. How this mdx is generated is also with cursor/Claude. When user clicks on "Next" the modal will show a prompt they can copy and paste to ai agent that will generate the mdx. This again creates a lot of flexibility. You can change the prompt, you can even directly ask cursor to reformat/change/modify the mdx. And the researcher hit refresh on the UI (*not the F5), it'll refresh. Note that the clips are nto going to auto load when land on the mdx rnedered view, teh clip will only start to load when teh "play" button is clicked on to reduce any glitches.

Another workflow is the tag board flow, once the findigns.md is done, the user can click on Tags (thers's a Findings/Tags toggle in the global header) to switch to the findings.md view, a modal show up with two prompts, one is to parse all of the clips in findings.md and reorganize by tags/codes accroding to the codebook. Teh codebook by default is global tag, but if customized codebook is loaded (in the set up stage), it'll use a consolidated codebook that includes both global and customized codebooks. Another promtp is to simplay re-read all of the trasncritps and regenerate the tags.md based on the codebook. Teh researcher pastes the prompt to cursor to work onteh tags.md. THe UI shows a blank page but tells uers to refresh (the refresh icon/button not f5) when cursor/claude is done. And they can do the same editing in raw md to update teh tags.md file.

Since we will show the tags/codes on those quote cards, we should also show that on teh trasncript, how I designed it is taht if a user hover over the quote card on the transcript view, the tas will pop up above the qutoe card in the trasncript like two bubbles sitting on the upper boarder of teh qutoe cards. Without hover over, it'll jsut be small colored dots on teh qutoe card on the trasncritp view. On teh rendered md view, it'll show the tags on the qutoe card. By double clicking on the quote card, it'll show a modal for user to change the tags (at least a qutoe can have up to three tags), chagne the duration, or even delete/hide the qutoe crad on teh trasncrript view. As I say, teh transcritp view is just a tool to create the findings/tags.md file. Change the tags/duration/hide it won't affect the md file until the user actually delete the previosu quote cards on teh findings.md or tags.md and then drag the new/updated quote from transcript to the md files. That's why we should explain that in the popup modal and use "hide" instead of delete quote.

The quote cards on teh transcritp view will play with single click, not double click and will stop by the  end of that qutoe. Same with the md rendered view. If click on the timestamp, play adn stop at the end of the clip.

Once the findigns.md looks good, the researcher feels that it is okay to publish, they will click on download html, which will turn the mdx in to html. It  will slice the clips based on the quote card info and imbed those clips inline. The researcher of course can ask cursor to modify the html by changing the look of it etc. But by default it's just pretty plain and simple with text, some formatting, emojis, information panels, and video players.  The html opens in a new tab.

Back to the report builder UI, there's also a publish report button that will show a cursor prompt to guide cursor to publish the report to a shared space like confluence or other space like Notion? I don't have enterprise notion not sure if I can get the API to do it, or even just google drive folder? Or even just the html if somewhere we can host the html adn clips. The cursor prompt will also update the published report's url in teh report.json file.  The confluenc or other space they choose should also have a research index page that the ai agent can update after uploading the report, with name, metadata, links to the report.

Last step is that the researcher can use another prompt for cursor (will show up once publish to confluence is done) upload the data (all reports, md, mdx,html files, clips) to a shared space, can be SharePoint or google drive etc. In the SharePoint folder, we also have a report viewer html that shows all pushed research projects to sharepoint, and the reports, tag boards, and links to confluence. To update this html, the last step in the report builder is that the cursor will run a js script to update that html.

The tags.md is the same except that it won't publish to sharepoint. You can click to download the html with the same process of slicing the clips and imbed in html (all cips wil lbe in the same clips folder for easy tracking), but there's no longer a publish button only the Save to sharepoint/googel drive or whatever server.

The last prompt will do both saving teh report data files and the tag board mdx, md, and html to the sharepoint folder.

For that report repo viewer, it'll read projects in the SharePoint folder and show those projects on the dashboard with title, metadata like persona, product name, date, and three links (one link to the finding html, one link to the tag board html, one to confluence/other place where report is published). We have a search (will search project name) and some filters such as prodcut, persona, date, researcher. The filter is on the left pane.

I'm hoping to have these links, the confluence space link, the report repo preview link (it'll prob use .env to store local path of the sharepoint since once the sharepoint folder is added a shortcut to onedrive it'll have a local path) linked from the reprot builder via a waffle menu too.

These are my current approaches, we might be able to streamline this even more now taht I'm working on my own version. But the #1 goal I'm trying to reach is the no extra service needed. Use your existing infra, whether it is sharepoint or google drive. No extra LLM API needed. A light-weight report analyzer and builder with the power of AI without having to pay those black box LLM models with limited flexibility from those SaaS companies