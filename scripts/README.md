# Scripts (moved to skills)

Scripts live in the corresponding skill folders:

- **Transcription**: `.cursor/skills/research-analysis/scripts/transcribe_groq.py`
- **Slice clips / export HTML**: `.cursor/skills/report-publication/scripts/slice-clips.js`, `export-reports-html.js`

Run via npm: `npm run slice-clips`, `npm run dev` (runs export on predev).
